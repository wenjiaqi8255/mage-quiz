---
phase: 06-json-driven-quiz-data
plan: 02
type: execute
wave: 1
depends_on: [06-01]
files_modified: []
autonomous: true
requirements: []
must_haves:
  truths:
    - "QuizContext loads questions from quizConfig.ts"
    - "Quiz page renders options from JSON (option.text)"
    - "Result page displays archetype from JSON"
  artifacts:
    - path: "src/context/QuizContext.tsx"
      provides: "Quiz state management with JSON data source"
      imports: "quizConfig.ts"
    - path: "src/pages/Quiz.tsx"
      provides: "Quiz UI rendering JSON questions"
      imports: "quizConfig.ts"
    - path: "src/pages/Result.tsx"
      provides: "Result display with JSON archetype"
      imports: "calculateResult"
  key_links:
    - from: "QuizContext.tsx"
      to: "quizConfig.ts"
      via: "import questions"
    - from: "Quiz.tsx"
      to: "quizConfig.ts"
      via: "import questions, dimensionLabels"
    - from: "Result.tsx"
      to: "calculateResult"
      via: "useQuiz hook"
---

<objective>
Update QuizContext and pages to use the JSON-driven quizConfig module instead of hardcoded data files.
</objective>

<execution_context>
@/Users/wenjiaqi/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/wenjiaqi/Downloads/mage/src/context/QuizContext.tsx (current context - uses questions from data/questions)
@/Users/wenjiaqi/Downloads/mage/src/pages/Quiz.tsx (current page - imports dimensionLabels from data/questions)
@/Users/wenjiaqi/Downloads/mage/src/data/quizConfig.ts (created in plan 01 - new typed exports)
</context>

<tasks>

<task type="auto">
  <name>Update QuizContext to use quizConfig</name>
  <read_first>
    - /Users/wenjiaqi/Downloads/mage/src/context/QuizContext.tsx (current implementation)
    - /Users/wenjiaqi/Downloads/mage/src/data/quizConfig.ts (new config module)
  </read_first>
  <action>
Update `src/context/QuizContext.tsx`:
1. Change import: `import { questions, type Dimension } from '../data/questions'`
   → `import { questions, dimensions, type Dimension } from '../data/quizConfig'`
2. The `Dimension` type is now exported from quizConfig as part of the question dimension field
3. Keep same Answers interface structure (src/met/cst arrays)
4. Keep same state structure and API - just change the data source
5. Verify dimensionLabels can be derived or imported - check quizConfig.dimensions keys for labels

Optional: Export dimensionLabels from quizConfig:
```typescript
export const dimensionLabels: Record<string, string> = {
  source: dimensions.source.label,
  method: dimensions.method.label,
  cost: dimensions.cost.label
}
```
  </action>
  <verify>
    <automated>grep -n "from.*quizConfig" /Users/wenjiaqi/Downloads/mage/src/context/QuizContext.tsx</automated>
  </verify>
  <acceptance_criteria>
    - QuizContext.tsx imports questions from quizConfig.ts (not data/questions.ts)
    - TypeScript compiles without errors
    - Quiz state works same as before (src/met/cst arrays, 9 questions)
  </acceptance_criteria>
  <done>QuizContext uses JSON-driven data source</done>
</task>

<task type="auto">
  <name>Update Quiz page to use quizConfig</name>
  <read_first>
    - /Users/wenjiaqi/Downloads/mage/src/pages/Quiz.tsx (current page)
    - /Users/wenjiaqi/Downloads/mage/src/data/quizConfig.ts (new config module)
  </read_first>
  <action>
Update `src/pages/Quiz.tsx`:
1. Change import: `import { questions, dimensionLabels } from '../data/questions'`
   → `import { questions, dimensionLabels } from '../data/quizConfig'`
2. If dimensionLabels not exported from quizConfig, add it in plan 01 first
3. The question structure from JSON has same fields needed: text, dimension, options[]
4. Options in JSON have `text` field - verify label maps correctly (JSON uses 'text', old used 'label')
5. Update option rendering if needed: option.label → option.text
  </action>
  <verify>
    <automated>grep -n "option\." /Users/wenjiaqi/Downloads/mage/src/pages/Quiz.tsx | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Quiz.tsx imports from quizConfig.ts
    - Options render correctly (using option.text from JSON)
    - Dimension labels display correctly (来源/方式/代价)
  </acceptance_criteria>
  <done>Quiz page renders questions from JSON config</done>
</task>

<task type="auto">
  <name>Update Result page to use JSON result format</name>
  <read_first>
    - /Users/wenjiaqi/Downloads/mage/src/pages/Result.tsx (current page)
    - /Users/wenjiaqi/Downloads/mage/src/utils/calculateResult.ts (updated in plan 01)
  </read_first>
  <action>
Update `src/pages/Result.tsx`:
1. Remove import: `import { questions, dimensionLabels } from '../data/questions'` (no longer needed)
2. Keep: `import { useQuiz } from '../context/QuizContext'`
3. Result object from calculateResult now has:
   - `symbol` (previously `symbol` field, JSON uses `rune`)
   - `name`, `description`, `source`, `method`, `cost`
   - `example` (from examples[0])
   - `scores`
4. Update display: result.symbol (already correct), result.example (if needed)
5. Result page should now work with updated calculateResult output
  </action>
  <verify>
    <automated>grep -n "result\." /Users/wenjiaqi/Downloads/mage/src/pages/Result.tsx | head -10</automated>
  </verify>
  <acceptance_criteria>
    - Result page displays archetype name, symbol, description from JSON
    - Score breakdown shows correctly (src/mets/cst scores)
    - Example/典型特征 shows correctly
  </acceptance_criteria>
  <done>Result page renders JSON-driven archetype data</done>
</task>

</tasks>

<verification>
- Run `npm run build` to ensure all components use new data source
- Test the app: landing → quiz → result flow works
</verification>

<success_criteria>
- All pages use quizConfig.ts as single source of truth
- Quiz flow (9 questions) works correctly
- Result shows correct archetype from JSON
</success_criteria>

<output>
After completion, create `.planning/phases/06-json-driven-quiz-data/06-02-SUMMARY.md`
</output>