---
phase: 06-json-driven-quiz-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: []
must_haves:
  truths:
    - "JSON configuration loads at runtime without errors"
    - "TypeScript recognizes all JSON fields with proper types"
    - "calculateResult uses direct key lookup from JSON"
  artifacts:
    - path: "src/data/quizConfig.ts"
      provides: "Type-safe JSON data exports"
      exports: ["questions", "archetypes", "dimensions", "scoring", "meta"]
    - path: "src/utils/calculateResult.ts"
      provides: "Result calculation using JSON key format"
      uses: "quizConfig.ts"
  key_links:
    - from: "src/data/quizConfig.ts"
      to: "mage_quiz_config.json"
      via: "import statement"
    - from: "src/utils/calculateResult.ts"
      to: "src/data/quizConfig.ts"
      via: "import archetypes"
---

<objective>
Load JSON configuration at runtime and create type-safe data utilities. This establishes the foundation for all subsequent JSON-driven work.
</objective>

<execution_context>
@/Users/wenjiaqi/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/wenjiaqi/Downloads/mage/mage_quiz_config.json (source of truth - full schema)
@/Users/wenjiaqi/Downloads/mage/src/data/questions.ts (current hardcoded - to be replaced)
</context>

<tasks>

<task type="auto">
  <name>Create JSON types and loading utility</name>
  <read_first>
    - /Users/wenjiaqi/Downloads/mage/mage_quiz_config.json (entire file - source of truth)
    - /Users/wenjiaqi/Downloads/mage/tsconfig.json (check path resolution)
  </read_first>
  <action>
Create `src/data/quizConfig.ts` with:
1. Type definitions matching JSON schema:
   - `QuizConfig` interface with `_meta`, `dimensions`, `questions`, `scoring`, `archetypes`
   - `Dimension` type: 'source' | 'method' | 'cost'
   - `Option` interface with `id`, `text`, `value`
   - `Question` interface with `id`, `dimension`, `index_in_dimension`, `design_angle`, `text`, `options[]`
   - `Archetype` interface with `name`, `rune`, `description`, `examples[]`, `tags[]`
2. Import and export the JSON data: `import config from '../../mage_quiz_config.json'` with proper path
3. Re-export typed data:
   - `export const questions: Question[] = config.questions`
   - `export const archetypes: Record<string, Archetype> = config.archetypes`
   - `export const dimensions = config.dimensions`
   - `export const scoring = config.scoring`
   - `export const meta = config._meta`

Note: If Vite doesn't support JSON import directly, add `"resolveJsonModule": true` to tsconfig.json compilerOptions.
  </action>
  <verify>
    <automated>grep -n "export const questions" /Users/wenjiaqi/Downloads/mage/src/data/quizConfig.ts</automated>
  </verify>
  <acceptance_criteria>
    - `/Users/wenjiaqi/Downloads/mage/src/data/quizConfig.ts` exists and exports `questions`, `archetypes`, `dimensions`, `scoring`, `meta`
    - TypeScript compiles without errors (tsc --noEmit)
    - Import paths in quizConfig.ts correctly reference mage_quiz_config.json
  </acceptance_criteria>
  <done>JSON loaded and typed, data accessible via quizConfig.ts</done>
</task>

<task type="auto">
  <name>Update calculateResult to use JSON key format</name>
  <read_first>
    - /Users/wenjiaqi/Downloads/mage/src/utils/calculateResult.ts (current implementation)
    - /Users/wenjiaqi/Downloads/mage/mage_quiz_config.json (scoring section - method, tiebreaker, key format)
  </read_first>
  <action>
Update `src/utils/calculateResult.ts`:
1. Import archetypes from new config: `import { archetypes } from '../data/quizConfig'`
2. Remove the old nameMap lookup - use key format directly from scoring: `{source}-{method}-{cost}`
3. Keep the tiebreaker logic: "first_question_wins" - if tie, use answer from index 0 of that dimension
4. Update Result interface to match JSON archetype format:
   - `name: string`
   - `symbol: string` (renamed from rune in JSON)
   - `description: string`
   - `source: string` (from dimension key)
   - `method: string`
   - `cost: string`
   - `example: string` (use examples[0] from JSON)
   - `scores: { src, met, cst }`
5. Return exact JSON archetype data with scores appended
  </action>
  <verify>
    <automated>grep -n "archetypes\[" /Users/wenjiaqi/Downloads/mage/src/utils/calculateResult.ts</automated>
  </verify>
  <acceptance_criteria>
    - calculateResult.ts imports from quizConfig.ts (not old archetypes.ts)
    - Key lookup uses string interpolation: `${topSrc}-${topMet}-${topCst}`
    - Result matches JSON archetype properties (name, symbol, description, source, method, cost, example)
  </acceptance_criteria>
  <done>Result calculation directly uses JSON key format without name mapping</done>
</task>

</tasks>

<verification>
- Run `npm run build` to ensure no TypeScript errors
- Verify JSON data loads correctly: check console for import errors
</verification>

<success_criteria>
- quizConfig.ts exports typed questions and archetypes from JSON
- calculateResult.ts uses JSON key format directly (no name mapping)
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-json-driven-quiz-data/06-01-SUMMARY.md`
</output>